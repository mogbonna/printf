#include "main.h"
/**
*print_strings - Print a string being passed as an argument
*arg_p: the argument passed to the printf
*Return: the length of the string
*/
int print_strings(va_list arg_p)
{
char *value;
int length = 0;
value = va_arg(arg_p, char *);
if (!value)
{
value = "(null)";
length;
return (length);
}
while (*value != '\0')
{

}
}
