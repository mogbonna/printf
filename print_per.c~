#include "main.h"
/**
 *print_per - prints the percent sign
 *@args: takes the argument passed
 *Return: 1
 */
int print_per(va_list args)
{
int num;
num = va_arg(args, int);
if (num != 0)
{
_putchar(37);
}
_putchar(37);
return (1);
}
